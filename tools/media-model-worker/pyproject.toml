[project]
name = "cloud-worker-media-model-worker"
version = "0.1.0"
description = "Dependency-free protocol worker boundary for content media generation"
requires-python = ">=3.11"
dependencies = []

[tool.pytest.ini_options]
testpaths = ["tests"]
