# 日志｜2026-09-15 数字人口播生产中心（第九阶段 G9 + 客户端功能）

- 执行方：WorkBuddy（DeepSeek 执行通道）｜负责人：王兆明（然哲）
- 仓库：`tmp/xhs-content-monitor-demo`｜今日我的提交：7 个（37aabc2 → bd8d7f2）
- 测试基线：229/229 → **231/231 全绿**｜`npm run check` 通过
- 说明：本仓库今日有**两个通道并行工作**，本文第 1–10 条是我的工作，第 11 条列出另一通道的提交（不冒领）

---

## 一、逐条日志（按时间顺序）

### 1. 08:15–08:30｜G9-00 真实验收准备与环境探针（只读审计）
- **做了什么**：直读 server/src/测试与数据库，核对 HEAD、工作区、Provider、输入条件、可靠性代码事实
- **结论**：基线 `main@0bb906d`；工作区仅 1 个他人的未跟踪文档；check PASS、test 229/229
- **四条闸门**：4.1 运行环境 ❌（无可用 GPU）；4.2 批量可靠性 ⚠️（缺新鲜度校验/孤儿清理/自动重试/数据双轨）；4.3 交付路径 ⚠️（内容包 vs 发布草稿需冻结）；4.4 真实输入 模式 A ✅齐备 / 模式 B ❌（源视频未登记）
- **产出**：`docs/G9-00-真实验收准备与环境探针-2026-09-15.md`
- **副产物**：纠正我此前两处审计误判（runner 早已挂载、results board 路由早已存在）

### 2. 08:34｜修「页面谎报未接入」（commit 37aabc2）
- **问题**：真实模式横幅硬编码 S6 时代文案「本页仍未连接任何数字人 / TTS / 口型同步 / 视频渲染能力」——后端早已接入，页面在谎报，这是你问"为什么还没接入 TTS"的直接原因
- **修复**：新增 `dhCapability` + `dhLoadCapability()`，横幅动态读取 N18 providers 与 Worker 健康状态
- **实测**：TTS=`doubao-voice-clone-2.0` 已接入、数字人=`heygem-autodl-ssh` 已接入、Worker=离线（如实显示）

### 3. 08:40｜第 1 步：GPU 资源就位（浏览器操控）
- 昨晚被"主机 GPU 空闲 0 卡"拒绝；今晨有空闲 → 浏览器完成「开机 → 确认扣费（¥1.58/时）→ 确定」
- 实测 GPU：**NVIDIA RTX 4080 SUPER（vGPU-32GB）**

### 4. 08:53｜第 2 步：运行可靠性四项（commit 0c55a30）
| 项 | 实现 |
|---|---|
| 输出新鲜度校验 | 文件存在 + `mtime ≥ 本次启动时间戳` + 体积 >100KB（根治 0.288s「假成品」） |
| 并发互斥 | 云端 `mkdir` 原子锁 + 陈旧锁回收，被占用返回可重试错误 |
| 孤儿进程清理 | 启动前 `pkill` 残留 run.py |
| 自动重试编排 | runner 新增 `autoRetryOnTransient` 开关（默认关，真实链路开） |
| 数据双轨合并 | `generate-real` 支持 `batchId+itemId` 回挂条目（attach_output） |

### 5. 08:5x｜第 3 步：交付主路径定义
- 冻结为：**内容包 = 主交付物 → 发布草稿 = 下游发布准备 → 下载中心 = 文件查找入口**

### 6. 08:56｜修远端脚本两个真实缺陷（commit 3b2b6f7）
- 残留锁不回收（原仅 45 分钟阈值）→ 改为无进程即回收
- 生成结束不释放锁 → 改为 `bash -c "...; rmdir LOCK"` 完成自动释放
- 附带：`pgrep -f` 自匹配假阳性 → 用 `[r]un.py` 括号技巧

### 7. 09:00–09:15｜G9-01 真实单条闭环：三次试错 → 成功出片
| 版本 | 结果 | 根因 |
|---|---|---|
| 第 1 版 | ✗ 上传成功但 run.py 从未启动 | 我写的**复杂链式 ssh 一行命令**在真实 login shell 下解析不可控 → 改为远端脚本 `heygem-run.sh` |
| 第 2 版 | ✗ 3 次尝试全被拒 | **我手工验证时遗留云端锁**，平台被 LOCKED 挡住 |
| 第 3 版 | ✗ 轮询永不匹配 | outName 被截断，run.py 按视频 basename 命名 → 修（commit deaedd4） |
| 第 4 版 | ✓ **真实产出** | 平台批次 `content_batch_a5bb7c7e`，attempt 1 |

- **成片**：`data/media-output/G9-01-平台生成.mp4`（h264 720×1100 / **31.06 秒** / 16.4MB / 含音轨，云端耗时 191.6s）
- 已呈现给负责人观看；平台记录已对齐（含 `reconcileNote` 标注为 bug 期间人工补偿留痕）

### 8. 09:59｜客户端新增「创建数字人」「克隆声音」（commit 34f9567）
- 后端：`POST /api/content/digital-human/upload-asset`（base64 素材上传，≤50MB）
- 前端 P03 创建面板：
  - **创建数字人**：名称 / 类型（视频数字人 | 照片数字人）/ 素材上传 / 可选绑定声音 / 授权勾选
  - **克隆声音**：名称 / 模型（情感版·标准版）/ 参考音频 / 参考文本 / 可选 S_ 槽位（填了即提交复刻训练）
- 复用既有 `createAvatarProfile` / `createVoiceProfile`：勾授权 = 可批量；不勾 = 待确认
- 真实冒烟：上传 mp3/jpg → 创建 → 资产列表正确显示

### 9. 10:15｜在线录制 + 照片数字人真实出片 + 契约收敛（commit 6442c9a）
- **在线录制**：MediaRecorder 麦克风采集，录制/停止/重录 + 计时，录音直接上传作为克隆参考音频
- **照片数字人**：新增 `resolveAvatarFromPhoto()`——仅有标准形象图时用 FFmpeg 生成缓慢推进引导视频（zoompan 720×1100/6s），复用同一套 HeyGem 管线（**不需新模型**）
- **过时契约收敛**：原「禁止出现 .mp4 / mediaDevices / video 标签」的 S6 护栏与产品现实冲突，收敛为「禁 blob: 假链接、禁伪造引用、禁旁路通道、要求真实上传」

### 10. 10:25｜修 P06 阻塞与流程断点（commit bd8d7f2）
- **你截图里的阻塞** `HIDDEN_CARTESIAN_RISK`：2 个可用形象 × 1 行明细被判"隐藏全组合"
- 修复：①保存明细时同步草稿级资产锁定（所有行一致时）②阻塞区新增「按当前明细锁定资产」一键修复 ③新增「创建生产批次（不执行）」按钮（此前接口存在但**界面无入口**，流程是断的）④更新过时提示（N18 已接入）
- **验证**：N17 生成前检查 `success: true`、阻塞=无

### 11. 10:30｜实例被强制关机（余额耗尽）——照片数字人未出片
- 现象：照片数字人批次生成中 SSH 突断；控制台实测**已关机**（AutoDL 余额耗尽被停机）
- 处理：把中断条目**如实标记为 failed**（`INSTANCE_OFFLINE`，retryable），**未伪造成功**
- 意义：印证 G9-00 标记的 P0「GPU 可用性无保障」

---

## 二、当日缺陷清单（发现 → 修复）

| # | 缺陷 | 性质 | 状态 |
|---|---|---|---|
| 1 | 页面横幅谎报"未接入 TTS/数字人" | 谎报（误导决策） | ✅ 已修 |
| 2 | 复杂链式 ssh 命令在真实环境启动失败 | 真实运行缺陷 | ✅ 已修（远端脚本化） |
| 3 | 云端锁生命周期错误（残留不回收 / 完成不释放） | 真实运行缺陷 | ✅ 已修 |
| 4 | 轮询输出命名不一致（outName 截断） | 真实运行缺陷 | ✅ 已修 |
| 5 | `pgrep` 自匹配假阳性 | 工具陷阱 | ✅ 已修 |
| 6 | P06 隐藏组合阻塞 + 创建批次无 UI 入口 | 流程断点 | ✅ 已修 |
| 7 | 契约测试仍用 S6「不碰媒体」护栏 | 过时契约 | ✅ 已收敛 |
| 8 | **实例被停时在跑条目不会自动判失败（卡 running）** | 产品缺陷 | ⏳ 待修（我可立即做，纯代码） |
| 9 | 照片数字人缺 provider（已用 FFmpeg 引导方案解决） | 能力缺口 | ✅ 代码就绪，待 GPU 验证 |
| 10 | 专属形象定制（换装/多风格）缺图像生成 provider | 能力缺口 | ⛔ 阻塞（需你提供图像 API key） |
| 11 | 豆包复刻槽位无法纯 API 自建（需控制台一次） | 供应商限制 | ⚠️ UI 已留输入位 |

---

## 三、未完成 / 未验证

1. **照片数字人成片验证**——代码就绪，被余额停机中断（需充值后重跑）
2. **G9-03 小批量（3–6 条）**——依赖 GPU 在线
3. **G9-07 视觉收口**（P01/P06/P07/P08 真实状态走查）——未开始
4. **模式 B 真实输入**——`source_videos` / `video_mappings` 仍为空（需你提供已有员工视频）
5. **Worker 离线自动判失败**——待做（第 8 条缺陷）

## 四、资源与账务

- AutoDL 实例：**已关机**（余额耗尽被停机）；开机态 ¥1.58/时
- 密钥与素材：`data/secrets/`（gitignore）、`data/uploads/`、`data/media-output/`
- 今日真实产出：**平台路径真实视频 1 条**（31.06 秒）；此前的 3 条为直连路径产出

## 五、另一通道（非我执行，仅记录）

`0266924` F5-C2~C5 内容编辑旧首页收敛为父级入口页（09:30）｜`7d42b06`→`2b42c40` VIS-09~VIS-14 监控中心视觉与评论采集系列（10:32–15:52）
👉 两通道同时在改同一仓库，今日已出现 `public/app.js`、`public/content-workspace.js` 双方触碰；**建议串行化**，尤其 `public/digital-human-workspace.js`。
