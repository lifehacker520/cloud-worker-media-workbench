# F5-C4 验收报告：第五阶段旧首页清理与 AI 数字人口播中心整合

| 项 | 值 |
|---|---|
| 任务编号 | F5-C4 |
| 项目路径 | `/Users/rancemac/Documents/obsidian/AI操作系统/tmp/xhs-content-monitor-demo` |
| 执行时间 | 2026-09-15 09:08 – 09:20 |
| 起始 HEAD | `3b2b6f7`（并行通道已提交 2 个 commit，见第 5 节） |
| 验收方式 | CDP 驱动真实浏览器（14 个场景）+ 实测交互 + 全量测试 + 代码审查 |
| 结果 | **PASS** — `npm run check` PASS，`npm test` **231/231 PASS**，控制台错误 **0** |

---

## 0. 结论速览

| 问题 | 结论 |
|---|---|
| 第五阶段清理是否完成 | **是**。旧首页 UI 与孤立 CSS 已全部下线 |
| 旧页面是否已退出正式产品路径 | **是**。`#content` 已是父级入口页，26 节点长页在 DOM 中零残留 |
| 新中心是否成为唯一正式功能入口 | **是**。内容编辑页只有 1 张入口卡，无重复入口；P01–P08 全部可打开 |
| 是否可进入下一阶段 | **可以**。存在 3 项遗留问题，均不阻塞（见第 6 节） |

---

## 1. 本轮修改（F5-C4）

本轮**不新增功能**，只修复"因第五阶段清理而失实的表述"。共 **4 个文件、9 处**：

| # | 文件 | 位置 | 原文（失实） | 改为 |
|---|---|---|---|---|
| 1 | `public/index.html` | L285 注释 | 「入口在内容编辑云员工工作区顶部」 | 说明入口由父级页提供、本模块只保留 `[data-dh-open]` 委托 |
| 2 | `public/index.html` | L288 `#view-publish` | 「发布准备已收纳到内容编辑工作区…**从内容任务的审核与打包节点进入**」 | 「发布准备保留独立入口」，去掉已不存在的节点路径 |
| 3 | `public/index.html` | L290 `#view-ai` | 「AI 能力已收纳到**内容编辑工作流**」 | 「AI 能力由内容编辑云员工与生产中心承担」 |
| 4 | `public/index.html` | L312 设置卡 | 「发布准备属于**工作流节点**」「任务与节点：**已接入 · 26 节点工作流**」+「可用」徽章 | 「内容编辑云员工是父级入口页…」；「生产中心：已接入 · P01–P08」 |
| 5 | `public/app.js` | L27 `VIEW_META.content` | 「从目标和素材开始，形成可审核、可交付的内容任务」 | 「内容编辑云员工的父级入口，进入下级 AI 数字人口播生产中心」 |
| 6 | `public/app.js` | L34 `VIEW_META['digital-human']` | 「**当前为演示骨架，未接入真实模型**」 | 「页面按真实接入状态显示可用能力」 |
| 7 | `public/app.js` | L39 `VIEW_META.publish` | 「发布准备已收纳到内容编辑工作流」 | 「发布准备保留独立入口，批准与真实发布分开」 |
| 8 | `public/content-workspace.js` | L203 页脚 | 「**26 节点长页**、素材解析、批量审核等旧首页表单已下线」 | 「口播视频生产在 AI 数字人口播生产中心完成」 |
| 9 | `README.md` | L69 | 「内容编辑云员工 P0 已建立 **26 节点工作流**」 | 说明已收敛为父级入口页、生产由生产中心承担、后端能力保留 |

**第 6 处是最严重的一处**：它把已接入真实能力的新中心描述为「演示骨架，未接入真实模型」，与并行通道刚提交的「修正能力状态谎报」属同类问题——反向误导。已改正。

**未改动**：`public/digital-human-workspace.js` 内部的 25 处"内容任务"表述——**全部是新中心的正确用法**（P01 关联真实内容任务），按你的要求未动。

---

## 2. 一、入口关系检查

| 链路 | 状态 | 证据 |
|---|---|---|
| 云员工工作台 → 内容编辑云员工 | **PASS** | 侧栏 `data-view="content"` 按钮；`app.js:1431` 角色绑定 |
| 内容编辑云员工 → 父级入口页 | **PASS** | `#content` → `view-content` 面板激活，页头显示「内容编辑云员工」 |
| 父级页 → AI 数字人口播生产中心 | **PASS** | `[data-dh-open]` 按钮存在；CW 与 DH 双向处理（幂等），跳转 `#digital-human` |
| AI 数字人口播生产中心 → P01 | **PASS** | 默认页 `p01`，`dhRenderReal` 正常渲染 |
| P01 → P02–P08 | **PASS** | 5 步导航（`[data-dh-page]` × 5）+ 页面常量 `p01/p02/assets/task/results/package` 全部实测可打开 |
| 返回路径 | **PASS** | DH 页头「返回内容编辑」(`data-dh-back`) 在 14 个场景中全部存在；返回后回到父级页 |
| 监控作品 → P02 | **PASS** | `app.js` `setView('digital-human')` → DH 监听 `content-work-prefill` → `dhGoToPage(DH_PAGE_P02)`；`monitor-ui-contract` M3 用例断言通过 |

---

## 3. 二、旧页面清理确认（CDP 实测）

在 **14 个场景**（4 宽度 × 父级页 / 6 个 DH 页面 / 3 宽度 × P01 / 监控中心）中，页内探针统计：

| 检查项 | 探针表达式 | 结果 |
|---|---|---|
| 不再显示旧的 26 节点长页面 | `document.querySelectorAll('.content-node-panel').length` | **全部 0** |
| 不再显示旧批量生产表单 | `.content-batch-panel, .content-batch-matrix` | **全部 0** |
| 不再显示旧的声音 A/B 表单 | `.content-voice-comparison, [data-voice-comparison-form]` | **全部 0** |
| 不再显示旧的单条生成入口 | `[data-media-node-form]` | **全部 0** |
| 不再显示重复的 AI 数字人口播入口 | `#view-content .dh-entry` 计数 | **恒为 1** |
| 不再出现无法点击的旧按钮 | `[data-start-content],[data-pause-content],[data-create-batch],[data-start-batch],[data-content-create-focus]` | **全部 0** |

**合计旧 UI 残留：14 个场景 × 0 处。**

---

## 4. 三、新中心功能确认

| 检查项 | 结果 | 证据 |
|---|---|---|
| P02 到 P08 页面能够打开 | **PASS** | CDP 逐一导航 6 个页面，`#view-digital-human` 面板均激活且 `innerHTML` 非空 |
| 空状态清楚 | **PASS** | `dhRenderCopy` / `dhRenderAssets` / `dhRenderPackage` 均有空状态块（如「还没有员工数字人档案」「还没有导入任何已有视频」） |
| 真实数据读取失败时有错误提示 | **PASS** | `dhErrorBlock` + 各页 `status === 'error'` 分支，文案含「页面不会用示例文案冒充真实文案库」 |
| 不用演示数据冒充真实数据 | **PASS** | 默认 `DH_DEFAULT_SCENARIO = 'real'`；`dhRealBanner()` 显示动态能力状态；演示场景需手动切换且数据带「（示例）」后缀 |
| **草稿保存后刷新仍然存在** | **PASS（实测）** | 见下 |
| 生产明细明确区分 1 条和多条 | **PASS** | 显式逐行模型；页面文案「一行对应一个输出，1 行就是单条生产」；preflight 判定「至少 1 行」 |
| 每一条结果有独立状态 | **PASS** | `dhResultCard` 每条独立显示生成/审核/交付三轴 |
| 失败可以重试 | **PASS** | `[data-dh-retry]` → `POST /batches/:id/items/:itemId/retry`；不可重试时 disabled + 原因 |
| 人工审核和导出闸门仍然存在 | **PASS** | `[data-dh-review]`（通过/需修改）+ `[data-dh-export]`（按 `group.eligibleCount` 控制） |
| 模拟输出不能被当成真实交付 | **PASS** | 内容包按 `includedCount`/`blockedCount` 分组（`src/content-package.mjs`），模拟输出被领域层拒绝 |

### 草稿持久化实测（CDP 真实交互）

```
1. 进入 P01，读取原始草稿
   title: "参考：入职发工作手机到底有啥用？ · 草稿"
   taskId: content_task_e3a98949-...（指向真实内容任务）
2. 写入唯一标记 "F5C4-持久化验证-1789435150659"，点击 [data-dh-save-draft]
3. 保存后提示：草稿已保存到本地数据存储（digital_human_drafts），刷新后仍然存在。
4. 强制刷新页面
5. 刷新后 title 仍为 "F5C4-持久化验证-1789435150659"   → PASS
6. 恢复原值并再次保存，确认回到 "参考：入职发工作手机到底有啥用？ · 草稿"
   未捕获异常：0
```

---

## 5. 四、页面质量检查

CDP 探针在 14 个场景中的实测结果：

| 检查项 | 结果 |
|---|---|
| 1440 宽度 | **PASS** |
| 1280 宽度 | **PASS** |
| 1024 宽度 | **PASS** |
| 200% 缩放（720 CSS px @ dsf 2） | **PASS** |
| **横向溢出**（`scrollWidth - clientWidth`） | **14 个场景全部为 0** |
| 页面空白 | **无**。父级页与 DH 面板 `innerHTML` 长度均远大于 0 |
| 文字溢出 | 12 处，**全部非本次清理造成**（逐项归因见下） |
| 按钮不可点击 | **无实际项**（探针报的「标为已读」为隐藏面板误报） |
| **浏览器控制台错误** | **0** |
| 未捕获异常 | **0** |
| 请求失败（Network.loadingFailed） | **0** |
| 返回路径错误 | **无** |

### 文字溢出逐项归因（12 处）

| 场景 | 数量 | 元素 | 归因 | 是否本次造成 |
|---|---|---|---|---|
| DH P01 @1024 / @200% | 3 + 3 | `.dh-axis-badge`「未选择交付」 | 窄屏下徽章文字放不下。样式来自 `digital-human-workspace.css`，**F5-C3 未触碰该文件** | **否** |
| 监控中心 @1440 | 7 | `.sr-only`（屏幕阅读器专用元素） | `sr-only` 本身就是 1px 裁剪隐藏，`scrollWidth > clientWidth` 是**预期行为**，属探针误报 | **否** |
| 监控中心 @1440 | 1 | `STRONG`「【个微管控-防飞单】工作手机彭经理」 | 账号名过长，既有 | **否** |

**反向验证**：`sr-only` 定义在 `public/styles.css` 中，**备份与现文件均为 1 处**，F5-C3 未删。

### 按钮不可点击的说明

探针列出 14 个场景各 1 个 `disabled` 且无 `title` 的按钮「标为已读」。经查：该按钮位于监控中心的**隐藏面板**内（`offsetParent === null`），探针未过滤不可见元素，属**误报**。页面无"点了没反应的旧按钮"。

---

## 6. 五、代码质量检查

| 检查项 | 结果 |
|---|---|
| `npm run check` | **PASS**（exit 0） |
| `npm test` | **231/231 PASS，0 fail**（125s） |
| 是否只修改了本次任务范围 | **是**。本轮仅 4 个文件 9 处文案 |
| 是否误改 `server.mjs` | **否**。`git diff --stat -- server.mjs` 为空 |
| 是否误改 `src/` 公共模块 | **否**。`git diff --stat -- src/` 为空 |
| 是否删除了未确认的文件 | **否**。未见任何 `D` 状态 |
| 是否引入新依赖 | **否**。`package.json` / `package-lock.json` 无改动 |
| 是否产生演示数据污染 | **否**。生产预览脚本先写后恢复，实测末态与初始一致 |

### 工作区文件状态

```
 M public/app.js                         ← F5-C2 改造 + 本轮 3 处文案
 M public/content-workspace.js           ← F5-C2 重写 + 本轮 1 处页脚
 M public/digital-human-workspace.js     ← F5-C2 入口卡改造
 M public/digital-human-workspace.css    ← F5-C2 新增样式
 M public/shell-v2.css                   ← F5-C3 CSS 清理
 M public/styles.css                     ← F5-C3 CSS 清理
 M test/{content-batch,content-workflow,digital-human,monitor,publish}-ui-contract.test.mjs
 M public/index.html                     ← 本轮 4 处文案
 M README.md                             ← 本轮 1 处
?? docs/ 下 5 份报告 + 1 份并行通道文档
```

**本轮起 HEAD 已从 `37aabc2` 变为 `3b2b6f7`**：并行通道提交了 2 个 commit（`0c55a30` 运行可靠性四项、`3b2b6f7` 修远端脚本缺陷）。**已核对：这 2 个 commit 只涉及 `server.mjs`、`src/autodl-worker.mjs`、`src/content-batch-runner.mjs`、`tools/media-model-worker/heygem-run.sh`，与本次的 11 个文件零重叠，无冲突风险。**

---

## 7. 未解决问题

| # | 问题 | 影响 | 建议 |
|---|---|---|---|
| U1 | **DH 徽章在 ≤1024px 或 200% 缩放下的轻微文字溢出**（`.dh-axis-badge`「未选择交付」，共 3 处 × 2 场景） | 窄屏视觉瑕疵，不阻塞功能 | 既有问题（来自 F5-03/F5-06 的 DH 样式）。按"只修复本次清理直接造成的问题"**未修**，建议单独立项 |
| U2 | **`#view-content` 的初始 loading 文案**仍为「正在打开内容编辑云员工工作区…」 | 仅在 JS 未执行时可见 | 表述仍成立（内容编辑确为工作区），未改 |
| U3 | **F5-C3 报告 D2**：`styles.css` 剩余非目标类未做使用审计 | 可能仍有其他死类 | 独立议题，不在本轮范围 |
| U4 | **F5-C0 的 B1–B10 仍未迁移**（创建内容任务、素材授权解析、选题闸门、任务级审核与版本修订、反馈、声音 A/B、发布草稿、运行回放、26 节点追踪、节点级执行） | 后端接口完整保留但无 UI 入口 | 需产品决策。**P05 模板配置的答案已明确：后端三处已支持 `backgroundRef`/`logoRef`/`introRef`/`outroRef`/`musicRef`/`captionFontName`，缺的只是 P05 的配置入口** |
| U5 | **11 个改动文件尚未提交** | 长期未提交有丢失风险 | 建议尽快提交，**精确指定文件名**（并行通道仍在活跃提交） |
| U6 | `#view-publish` 仍保留 `publish-workspace.js` 的加载与旧 empty 文案 | 该视图已改为兼容提示卡，`publish-workspace.js` 的渲染可能不显示 | 低优先，建议后续确认该脚本是否仍需加载 |

---

## 8. 是否可以进入下一阶段

**可以。**

| 门槛 | 状态 |
|---|---|
| 旧页面退出正式产品路径 | ✅ 已退出，DOM 零残留 |
| 新中心成为唯一正式功能入口 | ✅ 已确立，无重复入口 |
| 功能未因清理而缺失 | ✅ 后端能力与闸门完整，231 项测试全绿 |
| 页面质量达标 | ✅ 横向溢出 0、控制台错误 0、异常 0、请求失败 0 |
| 回归风险已收敛 | ✅ 断言目标已从 UI 层下移到后端层，清理不会静默破坏能力 |

**建议的下一阶段前置动作**（按优先级）：

1. **提交这 11 个文件**（精确指定，避免带上并行通道的文件）
2. 决策 U4：B1–B10 各自的去向（迁移 / 正式废弃）
3. 若采纳"补 P05 模板配置入口"，可直接复用后端已有的 `backgroundRef`/`logoRef`/`introRef`/`outroRef`/`musicRef`/`captionFontName`，**零后端改动**
4. 单独立项处理 U1（DH 窄屏徽章溢出）

---

## 附：验收方法与可复用产物

| 产物 | 路径 | 用途 |
|---|---|---|
| 逐页/逐宽度验收脚本 | `/tmp/f5c4-verify.mjs` | CDP 驱动，14 场景 × 12 项探针 + 截图 + 控制台采集 |
| 草稿持久化交互测试 | `/tmp/f5c4-draft-test.mjs` | 真实"写入→保存→刷新→校验→恢复"闭环 |
| 验收截图 | `/tmp/f5c4-shots/`（15 张） | 各页面各宽度留证 |
| 验收原始数据 | `/tmp/f5c4-verify.json` | 全部探针结果 |
| CSS 清理脚本 | `/tmp/f5c3-strip-css.py` | F5-C3 所用，带 4 道安全闸门 |

**方法要点记录**：本机此前 CDP 连续失败（`/json/list` 返回空 target）的根因是**端口被更早的僵尸 Chrome 实例占用**（`9333`），新实例绑定失败但被 `>/dev/null` 吞掉。改用全新端口 `9444` 后一次成功。**后续在此机器上做浏览器验收，应先确认端口空闲，不要复用旧端口。**

---

*状态：F5-C4 完成。第五阶段清理与整合验收通过。*
