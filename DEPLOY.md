# 云员工媒体工作台｜给客户使用的最短部署路径

当前版本已经是可运行的产品研发基线，适合先交付一个客户工作区：一个客户成员账号、一个管理员账号。客户可以维护客户/品牌上下文、加入监控账号、创建内容任务、解析授权素材、检索知识和提交反馈；管理员可以查看账号加入记录、刷新结果、数据看板、已读动作、内容运行事件和反馈。

当前文档只覆盖 Web 服务部署。桌面安装包由公开 GitHub 仓库的 Release 提供；GitHub 不承载运行数据，也不替代中央监控服务。

## 客户端与中央服务的关系

直接下载安装包时，客户端默认在客户电脑本地运行，每台电脑有自己的 data 数据目录。你要在自己的管理员端持续看到客户新增的账号和作品，需要先按下面的 B 路径部署一个中央服务，再让客户端通过用户数据目录里的 client-config.json 指向该服务：

    {"remoteUrl":"https://你的服务域名"}

这一步是“多人共用一个工作台”的前提；只把安装包发给客户，不会自动把客户电脑上的数据汇聚到你的电脑。

## 先理解两种分享方式

### A. 立刻给客户演示：临时公网地址

在你本机启动带认证的服务，再用 Cloudflare Quick Tunnel 临时转发：

    XHS_AUTH_REQUIRED=true XHS_AUTH_SECRET=自己生成的随机长字符串 XHS_ADMIN_PASSWORD=管理员密码 XHS_CLIENT_PASSWORD=客户密码 XHS_COOKIE_SECURE=false npm start

另开一个终端：

    cloudflared tunnel --url http://127.0.0.1:3188

它会生成一个随机的 trycloudflare.com 地址，把这个地址和客户密码发给客户即可。这个方式依赖你的电脑和终端一直在线，只适合短时间测试；不要把它当作正式客户服务。Cloudflare 官方也明确说明 Quick Tunnel 面向测试/开发，没有正式服务等级保障。

### B. 正式给一个客户试用：VPS/云服务器 + Docker + 域名 HTTPS

这是建议的最短正式路径：

1. 准备一台可以长期运行的 VPS/云服务器，安装 Docker、Docker Compose 和 Caddy。
2. 把整个 cloud-worker-media-workbench/ 目录复制到服务器。
3. 在服务器执行：

       cp .env.example .env

   编辑 .env，至少替换：

   - XHS_AUTH_SECRET：随机长字符串；
   - XHS_ADMIN_PASSWORD：你自己的管理员密码；
   - XHS_CLIENT_PASSWORD：给客户的成员密码；
   - XHS_REFRESH_MINUTES：服务端刷新间隔，试用可填 30；
   - XHS_COOKIE_SECURE=true：正式 HTTPS 保持为 true。

4. 启动并检查：

       docker compose up -d --build
       curl http://127.0.0.1:3188/api/health

   首次启动会从 config/accounts.seed.json 建立账号台账，并在后台执行一次刷新。data/ 在 Docker volume 中持久化，容器重启不会丢账号、作品、已读状态、活动和反馈。

5. 把域名的 DNS A 记录指向服务器 IP，并把 Caddyfile.example 复制为 Caddy 配置，把 radar.example.com 换成你的真实域名：

       radar.example.com {
           encode gzip
           reverse_proxy 127.0.0.1:3188
       }

   放行服务器 80/443 端口后，Caddy 会负责 HTTPS 证书和续期。完成后把 https://你的域名 发给客户。

## 交付后的使用闭环

1. 你用 admin 登录：查看账号台账、作品流、刷新活动和客户反馈。
2. 客户用 client 登录：添加/查看需要监控的公开主页，点击刷新，提交问题和功能建议。
3. 服务端按 XHS_REFRESH_MINUTES 自动刷新；客户不打开浏览器时也会继续运行。
4. 监控中心数据看板按已保存快照汇总播放、阅读、曝光、点赞、收藏、评论、分享和粉丝；平台卡片、发布状态和最新评论会随平台/周期筛选同步。
5. 你在本地修改代码，验证通过后重新构建发布客户端；中央 Web 服务则重新部署：

       docker compose up -d --build

   Docker volume 中的运行数据会保留。

   客户端发布命令：

       git tag v0.1.9
       git push origin v0.1.9

   GitHub Actions 会构建 macOS/Windows 安装包并更新 Release。客户打开客户端的“检查更新”即可获取新版本。

## 桌面端平台登录态

小红书、抖音和视频号的浏览器补采会使用三个固定且互相隔离的 Electron `persist:` 分区。登录信息由桌面端 Chromium 会话保存在当前用户的应用数据目录中，应用重启后会复用同一分区；服务端只接收解析后的账号、作品、指标和评论结果，不接收或导出 Cookie、密码或令牌。

首次补采或平台提示登录时，在弹出的对应平台窗口中完成登录/验证，再回到监控中心点击“补采”。设置 → 服务连接 → 平台登录态可以查看会话窗口是否打开、重新打开登录窗口，或由管理员点击“清除”来关闭窗口并清除该平台的站点数据。清除是显式操作，不会删除监控账号、作品或数据看板；清除后需要重新登录。页面显示“可复用”只表示该桌面分区存在，登录是否仍有效必须以平台页面实际提示为准。

## 内容编辑云员工的运行配置

服务启动后会在 data/ 中创建 `workbench.sqlite`。租户、项目、成员角色、连接器权限、内容任务、运行事件、媒体资产、知识文档和发布草稿以 SQLite 为权威数据；旧版 `content-tasks.json` 会被读取并作为兼容镜像保留。

生产环境建议在 `.env` 中明确设置：

- `XHS_DEFAULT_TENANT_ID`、`XHS_CLIENT_TENANT_ID`：客户工作区标识；不同客户不能复用同一个客户租户标识。
- `XHS_MEDIA_ROOTS`：允许素材解析的目录，多个目录用逗号分隔；不要开放整个服务器根目录。
- `DEEPSEEK_API_KEY`、`DEEPSEEK_MODEL`：启用真实模型的选题、脚本、平台适配和复盘生成；密钥只放服务器 `.env` 或正式密钥管理服务，不写入日志、知识库、前端或 Git。没有密钥时默认使用本地抽取式模板生成草案，明确标记为需人工审核；如需完全停用本地草案，可设置 `XHS_LOCAL_DRAFT_GENERATOR=false`。
- `XHS_TRANSCRIBE_COMMAND`：通用本地 ASR 命令，程序以无 shell 方式追加素材路径并读取 stdout；也可以改用 `XHS_WHISPER_COMMAND`、`XHS_WHISPER_MODEL` 和 `XHS_WHISPER_LANGUAGE`，由内置 `whisper-cli` 适配器为视频先提取 WAV 再转写。
- `XHS_OCR_COMMAND`：非 macOS 服务器的 OCR 命令；macOS 桌面版默认使用 Vision 识别首个关键帧。
- `XHS_BACKUP_ENCRYPTION_KEY`、`XHS_BACKUP_OFFSITE_DIR`、`XHS_BACKUP_RETENTION_COUNT` 或 `XHS_BACKUP_RETENTION_DAYS`：生产备份的强制安全配置。服务端会用 AES-256-GCM 加密快照，复制到与 `data/` 独立的目录，并按创建时间清理旧副本；缺任一控制项都会拒绝生产备份。密钥只放 `.env` 或正式密钥管理服务，不写入日志、前端和备份。

macOS 本地验收可以先安装 `whisper-cpp`，再下载一个兼容的 GGML 模型，并将模型绝对路径写入 `XHS_WHISPER_MODEL`；模型不应提交到代码仓库。服务会在运行时能力中分别显示 `whisper-cli` 和模型是否可用。

当前发布流程只会创建发布草稿并要求管理员批准；平台真实发布连接器尚未配置时，执行接口会明确拒绝，不会伪造发布成功。

## 工作台备份与校验

管理员可以在“工作区管理”中创建工作台备份。备份会先通过 SQLite `VACUUM INTO` 生成一致快照，再复制仍在兼容期使用的 JSON 台账，并写入 `manifest.json` 和文件指纹；默认不复制媒体文件。确实需要把媒体一并带走时，再勾选“包含媒体文件副本”，并预留足够磁盘空间。

备份创建不会覆盖当前数据库，也不会切换运行中的服务。生产环境创建后会将加密副本复制到 `XHS_BACKUP_OFFSITE_DIR`，管理员仍需在同一界面点击“校验”，确认 SQLite 完整性和副本指纹均通过。备份目录在服务端 `data/backups/<backupId>/`，不包含 `.env`、密码、Token 或 Cookie；异地目录必须由运维选择并限制访问。

已校验的本地备份可以先恢复到新的空数据目录进行抽样验收：

    npm run backup:workbench -- restore <backupId> --target /path/to/new-empty-data

恢复命令只写新的空目录，不覆盖当前运行目录，也不会自动让服务切换到恢复目录；加密备份会读取 `XHS_BACKUP_ENCRYPTION_KEY` 后再解密。若备份没有包含媒体文件，恢复结果会明确返回 `PARTIAL`，必须补齐媒体并重新验收后才能作为完整恢复使用。

## 上线前验收

- [ ] 用客户账号登录成功，能新增一个监控账号。
- [ ] 用管理员账号登录成功，能看到该账号的加入活动。
- [ ] 客户提交一条反馈，管理员观察区能看到。
- [ ] 手动刷新后，作品流和账号状态正常更新。
- [ ] 重启容器后，账号、作品、反馈仍然存在。
- [ ] 重启容器后，`workbench.sqlite` 中的内容任务、运行事件、媒体资产和知识文档仍然存在。
- [ ] 使用生产配置创建一次加密工作台备份，确认独立异地副本和保留策略生效，随后执行备份校验并确认 SQLite 完整性通过。
- [ ] 在隔离主机、独立空目录使用用户授权的客户生产快照执行恢复，并抽样验收账号、监控台账、内容任务、知识文档和媒体；本地自动化演练不能替代客户生产验收。
- [ ] 内容任务解析一个真实授权素材，能看到 ffprobe/ffmpeg/OCR/ASR 或明确的未配置状态；无 DeepSeek 密钥时仍能生成带素材依据的本地模板草案，且不会冒充 AI 结果；配置密钥后再用脱敏素材验收真实模型输出的来源表和人工审核边界。
- [ ] 客户成员只能看到自己的租户和项目，不能读取其他租户的内容任务或素材。
- [ ] 通过 HTTPS 域名访问，浏览器没有证书或登录循环问题。
- [ ] 已告知客户：当前只读取公开主页，不访问私密内容，不绕过验证码或登录限制。

## 必须知道的边界

- 当前认证仍是管理员/客户成员配置，租户和项目/RBAC 底座已经存在；成员、项目、邀请、组织目录、连接器和备份管理界面已接入。生产备份的加密、独立异地复制和保留策略已有服务端强制闸门，但真实客户异地介质、密钥保管/轮换、空间告警和生产恢复验收仍需由负责人提供并确认。
- 内容域数据和监控账号、作品、活动、反馈台账已统一写入 SQLite；原监控 JSON 仍保留为兼容镜像，启动时仅在 SQLite 监控表为空且存在旧 JSON 时导入。规模化部署前仍应完成异地/加密备份、保留策略和恢复演练。
- 服务端自动刷新是定时轮询，不等于平台实时推送；小红书页面结构或限流策略变化时，账号可能进入“需处理”。
- 数据看板只汇总已保存的采集快照；“—”表示该平台/作品尚未提供该指标，不会用抓取时间或估算值冒充真实数据。评论区当前为只读采集结果，不执行点赞、回复或发布。
- .env 含密码和签名密钥，不能提交到代码仓库或发给客户。
- 未签名的 macOS/Windows 安装包可能出现系统安全提示；正式规模化交付前应补充 Apple Developer / Windows 代码签名和公证。
